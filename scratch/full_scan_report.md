# Full Scan Report of docs/ folder

Total matches found: 82

| File | Line | Content | Matches |
| --- | --- | --- | --- |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L366) | 366 | `- `src/core/skillpack/&#123;manifest-v1,tarball,state,remote-source,trust-prompt,bootstrap-display,scaffold-third-party,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit&#125;.ts` + `examples/skillpack-reference/` + `docs/skillpack-anatomy.md` + `scripts/build-skillpack-anatomy.ts` — third-party skillpack ecosystem. `modusbrain skillpack scaffold &lt;owner/repo\|https-url\|./tgz\|./local-dir&gt;` resolves the spec via `classifySpec`, fetches through SSRF-hardened `git-remote.ts` (git) or extracts the tarball into `~/.modusbrain/skillpack-cache/&lt;host&gt;/&lt;owner&gt;/&lt;repo&gt;/&lt;sha&gt;/`, validates `skillpack.json` (api_version `gbrain-skillpack-v1`), checks `modusbrain_min_version`, surfaces a TOFU first-install identity-confirm prompt (author + source + pinned commit + tarball SHA + tier; non-TTY requires `--trust`), records the pin in machine-owned `~/.modusbrain/skillpack-state.json` (schema `modusbrain-skillpack-state-v1`, atomic `.tmp + rename`, `isAlreadyTrusted` skips re-prompt on author+pin match), runs through `enumerateScaffoldEntries` → `copyArtifacts` (one-time additive, refuses to overwrite), then DISPLAYS `runbooks/bootstrap.md` WITHOUT executing (deliberately does not auto-execute). Registry catalog at `thebuildceo/modusbrain-skillpack-registry` split into `registry.json` (PR-able, `modusbrain-registry-v1`) + `endorsements.json` (Admin-only overlay, `modusbrain-endorsements-v1`); `effectiveTier` merges. `registry-client.ts` fetches both via `If-None-Match` etag with 1h soft-TTL + stale-fallback (origins `fresh_fetch \| cache_warm \| cache_soft_stale \| cache_hard_stale`); hard-fail only on no-cache + no-network. CLI: `modusbrain skillpack &#123;search,info,registry,doctor,init,pack,endorse&#125;`. Doctor walks `SKILLPACK_RUBRIC_V1` (10 binary dimensions: 5 required CORE — manifest_valid, skills_have_skill_md, routing_evals_present ≥5 intents, skills_have_unique_triggers MECE, changelog_present_and_current — and 5 quality BADGES — unit_tests_present, e2e_tests_present, llm_eval_present ≥3 cases, bootstrap_runbook_present, license_present); tier eligibility: `endorsed` needs all 10, `community` needs core + ≥3 badges, `experimental` needs core only, `blocked` when any core fails. `--quick` ~5s structural sweep; `--fix --yes` auto-scaffolds `auto_fixable: true` dimensions and refuses to overwrite files whose mtime is newer than `skillpack.json`. `modusbrain skillpack init &lt;name&gt;` lands 11 files (skillpack.json, SKILL.md, routing-eval.jsonl, test/example.test.ts, e2e/example.e2e.test.ts, evals/example.judge.json, runbooks/&#123;bootstrap,uninstall,upgrade-template&#125;.md, CHANGELOG, README, LICENSE); freshly-init'd scores 10/10; `--minimal` skips test/e2e/evals. `modusbrain skillpack pack` packs a deterministic tarball via GNU tar (`--sort=name --mtime=@0 --owner=0 --group=0 --numeric-owner` + `GZIP=-n` + `TZ=UTC`); refuses on `tier_eligibility === 'blocked'`. Extract caps (5000 files / 100MB total / 1MB per file / 255-char paths / 100:1 compression ratio); rejects symlinks/hardlinks/devices/FIFOs. `modusbrain skillpack endorse &lt;name&gt; [--tier ...] [--push] [--dry-run]` runs in a clone of the registry repo: validates the pack in `registry.json`, mutates `endorsements.json` via pure `applyEndorsement`, stable-key-orders the write, commits `endorse: &lt;name&gt; -> &lt;tier&gt;`, optionally pushes. JSONL audit at `~/.modusbrain/audit/skillpack-YYYY-Www.jsonl` (ISO-week rotated, honors `MODUSBRAIN_AUDIT_DIR`). `examples/skillpack-reference/` is a 10/10 reference pack pinned by `test/e2e/skillpack-third-party.test.ts`. `docs/skillpack-anatomy.md` auto-generated via `scripts/build-skillpack-anatomy.ts` (`--check` for CI drift). CLI dispatch in `src/commands/skillpack.ts` disambiguates third-party (contains `/`, `://`, `.tgz`) from bundled-skill kebab; kebab routes bundled-first, registry-fallback. Tests: `test/skillpack-&#123;manifest-v1,tarball,state,remote-source,trust-prompt,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit,scaffold-third-party&#125;.test.ts` + `test/e2e/skillpack-third-party.test.ts`. Spec at `docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md`.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L408) | 408 | `- `src/commands/extract-conversation-facts.ts` + `src/core/cycle/conversation-facts-backfill.ts` — bulk fact extraction for long-form conversation pages. Walks conversation/meeting/slack/email pages, splits into time-windowed segments (30-min gap or 30-msg cap), prepends a topical/temporal header, and runs through `extractFactsFromTurn()` so anchor-rich facts surface in `modusbrain search`. Invariants: **strict per-source core** (`runExtractConversationFactsCore(&#123;sourceId, ...&#125;)` always takes one sourceId; CLI + cycle phase each do their own multi-source iteration because PHASE_SCOPE='source' is taxonomy-only); **two-phase enumeration** (paginated `listPages(&#123;type, sourceId, limit:10&#125;)`; per-page body cap MAX_PAGE_BODY_BYTES=25MB with `pages_skipped_too_large` counter surfaced in doctor); **page-global row_num accumulator** (facts unique index is `(source_id, source_markdown_slug, row_num)` per migration v51 — per-segment row_num would collide); **page-level TERMINAL audit row** to facts table after all segments commit (source='cli:extract-conversation-facts:terminal'; doctor's NOT EXISTS matches the terminal row so partial-extraction pages stay in backlog); **optional `opts.budgetTracker?`** (when present, used as-is — nested `withBudgetTracker` REPLACES; when absent, core auto-wraps with `BudgetTracker(&#123;maxCostUsd&#125;)`); **body read covers compiled_truth + timeline**; **honors `facts.extraction_enabled` kill-switch** with `--override-disabled` escape; **--types LIST allowlist** (`conversation,meeting,slack,email`) with CLI default reading `cycle.conversation_facts_backfill.types`; **fingerprint on sourceId only**; **string-encoded op-checkpoint** entries `"&lt;sourceId&gt;\|&lt;slug&gt;\|&lt;endIso&gt;"` for resume (durable audit is the facts terminal row); **`--background` via maybeBackground** (Minion handler `extract-conversation-facts` re-creates BudgetTracker from `data.max_cost_usd`; on `BudgetExhausted` mid-job catches + persists + marks `completed` with `result.budget_exhausted=true`). The companion cycle phase `conversation_facts_backfill` (default OFF) iterates `listSources(engine)`, creates ONE brain-wide tracker per tick + wraps the loop in `withBudgetTracker` + passes the tracker into every per-source call. Two-layer cost AND walltime caps: per-source (`max_cost_usd=$1`, `max_walltime_min=20`) AND brain-wide (`max_total_cost_usd=$5`, `max_total_walltime_min=30`). Pinned by `test/extract-conversation-facts.test.ts` (27 cases). Migration v94 adds partial index `idx_facts_extract_conversation_session ON facts(source_id, source_session) WHERE source LIKE 'cli:extract-conversation-facts%'` (`transaction:false` + invalid-index pre-drop on Postgres; plain CREATE INDEX on PGLite). `src/commands/doctor.ts:computeConversationFactsBacklogCheck` is 3-state (SKIPPED when disabled; OK when caught up; WARN when &gt;10 pages lack the terminal row, with paste-ready `modusbrain doctor --remediate` step). `src/commands/sources.ts:runAudit` adds `facts_backfill_estimate: &#123;pages, est_segments, est_cost_usd, types&#125;`. Schema-pack `gbrain-base.yaml` promotes `conversation` (temporal, extractable) + `atom` (annotation, NOT extractable) into the base seed; backstop uses hardcoded `ELIGIBLE_TYPES` in `src/core/facts/eligibility.ts:51` not pack extractable. `ALL_PAGE_TYPES` in `src/core/types.ts` extended with the two new types.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L774) | 774 | `- `src/core/skillpack/&#123;manifest-v1,tarball,state,remote-source,trust-prompt,bootstrap-display,scaffold-third-party,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit&#125;.ts` + `examples/skillpack-reference/` + `docs/skillpack-anatomy.md` + `scripts/build-skillpack-anatomy.ts` — third-party skillpack ecosystem. `modusbrain skillpack scaffold &lt;owner/repo\|https-url\|./tgz\|./local-dir&gt;` resolves the spec via `classifySpec`, fetches through SSRF-hardened `git-remote.ts` (git) or extracts the tarball into `~/.modusbrain/skillpack-cache/&lt;host&gt;/&lt;owner&gt;/&lt;repo&gt;/&lt;sha&gt;/`, validates `skillpack.json` (api_version `gbrain-skillpack-v1`), checks `modusbrain_min_version`, surfaces a TOFU first-install identity-confirm prompt (author + source + pinned commit + tarball SHA + tier; non-TTY requires `--trust`), records the pin in machine-owned `~/.modusbrain/skillpack-state.json` (schema `modusbrain-skillpack-state-v1`, atomic `.tmp + rename`, `isAlreadyTrusted` skips re-prompt on author+pin match), runs through `enumerateScaffoldEntries` → `copyArtifacts` (one-time additive, refuses to overwrite), then DISPLAYS `runbooks/bootstrap.md` WITHOUT executing (deliberately does not auto-execute). Registry catalog at `thebuildceo/modusbrain-skillpack-registry` split into `registry.json` (PR-able, `modusbrain-registry-v1`) + `endorsements.json` (Admin-only overlay, `modusbrain-endorsements-v1`); `effectiveTier` merges. `registry-client.ts` fetches both via `If-None-Match` etag with 1h soft-TTL + stale-fallback (origins `fresh_fetch \| cache_warm \| cache_soft_stale \| cache_hard_stale`); hard-fail only on no-cache + no-network. CLI: `modusbrain skillpack &#123;search,info,registry,doctor,init,pack,endorse&#125;`. Doctor walks `SKILLPACK_RUBRIC_V1` (10 binary dimensions: 5 required CORE — manifest_valid, skills_have_skill_md, routing_evals_present ≥5 intents, skills_have_unique_triggers MECE, changelog_present_and_current — and 5 quality BADGES — unit_tests_present, e2e_tests_present, llm_eval_present ≥3 cases, bootstrap_runbook_present, license_present); tier eligibility: `endorsed` needs all 10, `community` needs core + ≥3 badges, `experimental` needs core only, `blocked` when any core fails. `--quick` ~5s structural sweep; `--fix --yes` auto-scaffolds `auto_fixable: true` dimensions and refuses to overwrite files whose mtime is newer than `skillpack.json`. `modusbrain skillpack init &lt;name&gt;` lands 11 files (skillpack.json, SKILL.md, routing-eval.jsonl, test/example.test.ts, e2e/example.e2e.test.ts, evals/example.judge.json, runbooks/&#123;bootstrap,uninstall,upgrade-template&#125;.md, CHANGELOG, README, LICENSE); freshly-init'd scores 10/10; `--minimal` skips test/e2e/evals. `modusbrain skillpack pack` packs a deterministic tarball via GNU tar (`--sort=name --mtime=@0 --owner=0 --group=0 --numeric-owner` + `GZIP=-n` + `TZ=UTC`); refuses on `tier_eligibility === 'blocked'`. Extract caps (5000 files / 100MB total / 1MB per file / 255-char paths / 100:1 compression ratio); rejects symlinks/hardlinks/devices/FIFOs. `modusbrain skillpack endorse &lt;name&gt; [--tier ...] [--push] [--dry-run]` runs in a clone of the registry repo: validates the pack in `registry.json`, mutates `endorsements.json` via pure `applyEndorsement`, stable-key-orders the write, commits `endorse: &lt;name&gt; -> &lt;tier&gt;`, optionally pushes. JSONL audit at `~/.modusbrain/audit/skillpack-YYYY-Www.jsonl` (ISO-week rotated, honors `MODUSBRAIN_AUDIT_DIR`). `examples/skillpack-reference/` is a 10/10 reference pack pinned by `test/e2e/skillpack-third-party.test.ts`. `docs/skillpack-anatomy.md` auto-generated via `scripts/build-skillpack-anatomy.ts` (`--check` for CI drift). CLI dispatch in `src/commands/skillpack.ts` disambiguates third-party (contains `/`, `://`, `.tgz`) from bundled-skill kebab; kebab routes bundled-first, registry-fallback. Tests: `test/skillpack-&#123;manifest-v1,tarball,state,remote-source,trust-prompt,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit,scaffold-third-party&#125;.test.ts` + `test/e2e/skillpack-third-party.test.ts`. Spec at `docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md`.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L816) | 816 | `- `src/commands/extract-conversation-facts.ts` + `src/core/cycle/conversation-facts-backfill.ts` — bulk fact extraction for long-form conversation pages. Walks conversation/meeting/slack/email pages, splits into time-windowed segments (30-min gap or 30-msg cap), prepends a topical/temporal header, and runs through `extractFactsFromTurn()` so anchor-rich facts surface in `modusbrain search`. Invariants: **strict per-source core** (`runExtractConversationFactsCore(&#123;sourceId, ...&#125;)` always takes one sourceId; CLI + cycle phase each do their own multi-source iteration because PHASE_SCOPE='source' is taxonomy-only); **two-phase enumeration** (paginated `listPages(&#123;type, sourceId, limit:10&#125;)`; per-page body cap MAX_PAGE_BODY_BYTES=25MB with `pages_skipped_too_large` counter surfaced in doctor); **page-global row_num accumulator** (facts unique index is `(source_id, source_markdown_slug, row_num)` per migration v51 — per-segment row_num would collide); **page-level TERMINAL audit row** to facts table after all segments commit (source='cli:extract-conversation-facts:terminal'; doctor's NOT EXISTS matches the terminal row so partial-extraction pages stay in backlog); **optional `opts.budgetTracker?`** (when present, used as-is — nested `withBudgetTracker` REPLACES; when absent, core auto-wraps with `BudgetTracker(&#123;maxCostUsd&#125;)`); **body read covers compiled_truth + timeline**; **honors `facts.extraction_enabled` kill-switch** with `--override-disabled` escape; **--types LIST allowlist** (`conversation,meeting,slack,email`) with CLI default reading `cycle.conversation_facts_backfill.types`; **fingerprint on sourceId only**; **string-encoded op-checkpoint** entries `"&lt;sourceId&gt;\|&lt;slug&gt;\|&lt;endIso&gt;"` for resume (durable audit is the facts terminal row); **`--background` via maybeBackground** (Minion handler `extract-conversation-facts` re-creates BudgetTracker from `data.max_cost_usd`; on `BudgetExhausted` mid-job catches + persists + marks `completed` with `result.budget_exhausted=true`). The companion cycle phase `conversation_facts_backfill` (default OFF) iterates `listSources(engine)`, creates ONE brain-wide tracker per tick + wraps the loop in `withBudgetTracker` + passes the tracker into every per-source call. Two-layer cost AND walltime caps: per-source (`max_cost_usd=$1`, `max_walltime_min=20`) AND brain-wide (`max_total_cost_usd=$5`, `max_total_walltime_min=30`). Pinned by `test/extract-conversation-facts.test.ts` (27 cases). Migration v94 adds partial index `idx_facts_extract_conversation_session ON facts(source_id, source_session) WHERE source LIKE 'cli:extract-conversation-facts%'` (`transaction:false` + invalid-index pre-drop on Postgres; plain CREATE INDEX on PGLite). `src/commands/doctor.ts:computeConversationFactsBacklogCheck` is 3-state (SKIPPED when disabled; OK when caught up; WARN when &gt;10 pages lack the terminal row, with paste-ready `modusbrain doctor --remediate` step). `src/commands/sources.ts:runAudit` adds `facts_backfill_estimate: &#123;pages, est_segments, est_cost_usd, types&#125;`. Schema-pack `gbrain-base.yaml` promotes `conversation` (temporal, extractable) + `atom` (annotation, NOT extractable) into the base seed; backstop uses hardcoded `ELIGIBLE_TYPES` in `src/core/facts/eligibility.ts:51` not pack extractable. `ALL_PAGE_TYPES` in `src/core/types.ts` extended with the two new types.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L1274) | 1274 | `- `src/core/skillpack/&#123;manifest-v1,tarball,state,remote-source,trust-prompt,bootstrap-display,scaffold-third-party,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit&#125;.ts` + `examples/skillpack-reference/` + `docs/skillpack-anatomy.md` + `scripts/build-skillpack-anatomy.ts` — third-party skillpack ecosystem. `modusbrain skillpack scaffold &lt;owner/repo\|https-url\|./tgz\|./local-dir&gt;` resolves the spec via `classifySpec`, fetches through SSRF-hardened `git-remote.ts` (git) or extracts the tarball into `~/.modusbrain/skillpack-cache/&lt;host&gt;/&lt;owner&gt;/&lt;repo&gt;/&lt;sha&gt;/`, validates `skillpack.json` (api_version `gbrain-skillpack-v1`), checks `modusbrain_min_version`, surfaces a TOFU first-install identity-confirm prompt (author + source + pinned commit + tarball SHA + tier; non-TTY requires `--trust`), records the pin in machine-owned `~/.modusbrain/skillpack-state.json` (schema `modusbrain-skillpack-state-v1`, atomic `.tmp + rename`, `isAlreadyTrusted` skips re-prompt on author+pin match), runs through `enumerateScaffoldEntries` → `copyArtifacts` (one-time additive, refuses to overwrite), then DISPLAYS `runbooks/bootstrap.md` WITHOUT executing (deliberately does not auto-execute). Registry catalog at `thebuildceo/modusbrain-skillpack-registry` split into `registry.json` (PR-able, `modusbrain-registry-v1`) + `endorsements.json` (Admin-only overlay, `modusbrain-endorsements-v1`); `effectiveTier` merges. `registry-client.ts` fetches both via `If-None-Match` etag with 1h soft-TTL + stale-fallback (origins `fresh_fetch \| cache_warm \| cache_soft_stale \| cache_hard_stale`); hard-fail only on no-cache + no-network. CLI: `modusbrain skillpack &#123;search,info,registry,doctor,init,pack,endorse&#125;`. Doctor walks `SKILLPACK_RUBRIC_V1` (10 binary dimensions: 5 required CORE — manifest_valid, skills_have_skill_md, routing_evals_present ≥5 intents, skills_have_unique_triggers MECE, changelog_present_and_current — and 5 quality BADGES — unit_tests_present, e2e_tests_present, llm_eval_present ≥3 cases, bootstrap_runbook_present, license_present); tier eligibility: `endorsed` needs all 10, `community` needs core + ≥3 badges, `experimental` needs core only, `blocked` when any core fails. `--quick` ~5s structural sweep; `--fix --yes` auto-scaffolds `auto_fixable: true` dimensions and refuses to overwrite files whose mtime is newer than `skillpack.json`. `modusbrain skillpack init &lt;name&gt;` lands 11 files (skillpack.json, SKILL.md, routing-eval.jsonl, test/example.test.ts, e2e/example.e2e.test.ts, evals/example.judge.json, runbooks/&#123;bootstrap,uninstall,upgrade-template&#125;.md, CHANGELOG, README, LICENSE); freshly-init'd scores 10/10; `--minimal` skips test/e2e/evals. `modusbrain skillpack pack` packs a deterministic tarball via GNU tar (`--sort=name --mtime=@0 --owner=0 --group=0 --numeric-owner` + `GZIP=-n` + `TZ=UTC`); refuses on `tier_eligibility === 'blocked'`. Extract caps (5000 files / 100MB total / 1MB per file / 255-char paths / 100:1 compression ratio); rejects symlinks/hardlinks/devices/FIFOs. `modusbrain skillpack endorse &lt;name&gt; [--tier ...] [--push] [--dry-run]` runs in a clone of the registry repo: validates the pack in `registry.json`, mutates `endorsements.json` via pure `applyEndorsement`, stable-key-orders the write, commits `endorse: &lt;name&gt; -> &lt;tier&gt;`, optionally pushes. JSONL audit at `~/.modusbrain/audit/skillpack-YYYY-Www.jsonl` (ISO-week rotated, honors `MODUSBRAIN_AUDIT_DIR`). `examples/skillpack-reference/` is a 10/10 reference pack pinned by `test/e2e/skillpack-third-party.test.ts`. `docs/skillpack-anatomy.md` auto-generated via `scripts/build-skillpack-anatomy.ts` (`--check` for CI drift). CLI dispatch in `src/commands/skillpack.ts` disambiguates third-party (contains `/`, `://`, `.tgz`) from bundled-skill kebab; kebab routes bundled-first, registry-fallback. Tests: `test/skillpack-&#123;manifest-v1,tarball,state,remote-source,trust-prompt,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit,scaffold-third-party&#125;.test.ts` + `test/e2e/skillpack-third-party.test.ts`. Spec at `docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md`.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L1316) | 1316 | `- `src/commands/extract-conversation-facts.ts` + `src/core/cycle/conversation-facts-backfill.ts` — bulk fact extraction for long-form conversation pages. Walks conversation/meeting/slack/email pages, splits into time-windowed segments (30-min gap or 30-msg cap), prepends a topical/temporal header, and runs through `extractFactsFromTurn()` so anchor-rich facts surface in `modusbrain search`. Invariants: **strict per-source core** (`runExtractConversationFactsCore(&#123;sourceId, ...&#125;)` always takes one sourceId; CLI + cycle phase each do their own multi-source iteration because PHASE_SCOPE='source' is taxonomy-only); **two-phase enumeration** (paginated `listPages(&#123;type, sourceId, limit:10&#125;)`; per-page body cap MAX_PAGE_BODY_BYTES=25MB with `pages_skipped_too_large` counter surfaced in doctor); **page-global row_num accumulator** (facts unique index is `(source_id, source_markdown_slug, row_num)` per migration v51 — per-segment row_num would collide); **page-level TERMINAL audit row** to facts table after all segments commit (source='cli:extract-conversation-facts:terminal'; doctor's NOT EXISTS matches the terminal row so partial-extraction pages stay in backlog); **optional `opts.budgetTracker?`** (when present, used as-is — nested `withBudgetTracker` REPLACES; when absent, core auto-wraps with `BudgetTracker(&#123;maxCostUsd&#125;)`); **body read covers compiled_truth + timeline**; **honors `facts.extraction_enabled` kill-switch** with `--override-disabled` escape; **--types LIST allowlist** (`conversation,meeting,slack,email`) with CLI default reading `cycle.conversation_facts_backfill.types`; **fingerprint on sourceId only**; **string-encoded op-checkpoint** entries `"&lt;sourceId&gt;\|&lt;slug&gt;\|&lt;endIso&gt;"` for resume (durable audit is the facts terminal row); **`--background` via maybeBackground** (Minion handler `extract-conversation-facts` re-creates BudgetTracker from `data.max_cost_usd`; on `BudgetExhausted` mid-job catches + persists + marks `completed` with `result.budget_exhausted=true`). The companion cycle phase `conversation_facts_backfill` (default OFF) iterates `listSources(engine)`, creates ONE brain-wide tracker per tick + wraps the loop in `withBudgetTracker` + passes the tracker into every per-source call. Two-layer cost AND walltime caps: per-source (`max_cost_usd=$1`, `max_walltime_min=20`) AND brain-wide (`max_total_cost_usd=$5`, `max_total_walltime_min=30`). Pinned by `test/extract-conversation-facts.test.ts` (27 cases). Migration v94 adds partial index `idx_facts_extract_conversation_session ON facts(source_id, source_session) WHERE source LIKE 'cli:extract-conversation-facts%'` (`transaction:false` + invalid-index pre-drop on Postgres; plain CREATE INDEX on PGLite). `src/commands/doctor.ts:computeConversationFactsBacklogCheck` is 3-state (SKIPPED when disabled; OK when caught up; WARN when &gt;10 pages lack the terminal row, with paste-ready `modusbrain doctor --remediate` step). `src/commands/sources.ts:runAudit` adds `facts_backfill_estimate: &#123;pages, est_segments, est_cost_usd, types&#125;`. Schema-pack `gbrain-base.yaml` promotes `conversation` (temporal, extractable) + `atom` (annotation, NOT extractable) into the base seed; backstop uses hardcoded `ELIGIBLE_TYPES` in `src/core/facts/eligibility.ts:51` not pack extractable. `ALL_PAGE_TYPES` in `src/core/types.ts` extended with the two new types.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L1682) | 1682 | `- `src/core/skillpack/&#123;manifest-v1,tarball,state,remote-source,trust-prompt,bootstrap-display,scaffold-third-party,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit&#125;.ts` + `examples/skillpack-reference/` + `docs/skillpack-anatomy.md` + `scripts/build-skillpack-anatomy.ts` — third-party skillpack ecosystem. `modusbrain skillpack scaffold &lt;owner/repo\|https-url\|./tgz\|./local-dir&gt;` resolves the spec via `classifySpec`, fetches through SSRF-hardened `git-remote.ts` (git) or extracts the tarball into `~/.modusbrain/skillpack-cache/&lt;host&gt;/&lt;owner&gt;/&lt;repo&gt;/&lt;sha&gt;/`, validates `skillpack.json` (api_version `gbrain-skillpack-v1`), checks `modusbrain_min_version`, surfaces a TOFU first-install identity-confirm prompt (author + source + pinned commit + tarball SHA + tier; non-TTY requires `--trust`), records the pin in machine-owned `~/.modusbrain/skillpack-state.json` (schema `modusbrain-skillpack-state-v1`, atomic `.tmp + rename`, `isAlreadyTrusted` skips re-prompt on author+pin match), runs through `enumerateScaffoldEntries` → `copyArtifacts` (one-time additive, refuses to overwrite), then DISPLAYS `runbooks/bootstrap.md` WITHOUT executing (deliberately does not auto-execute). Registry catalog at `thebuildceo/modusbrain-skillpack-registry` split into `registry.json` (PR-able, `modusbrain-registry-v1`) + `endorsements.json` (Admin-only overlay, `modusbrain-endorsements-v1`); `effectiveTier` merges. `registry-client.ts` fetches both via `If-None-Match` etag with 1h soft-TTL + stale-fallback (origins `fresh_fetch \| cache_warm \| cache_soft_stale \| cache_hard_stale`); hard-fail only on no-cache + no-network. CLI: `modusbrain skillpack &#123;search,info,registry,doctor,init,pack,endorse&#125;`. Doctor walks `SKILLPACK_RUBRIC_V1` (10 binary dimensions: 5 required CORE — manifest_valid, skills_have_skill_md, routing_evals_present ≥5 intents, skills_have_unique_triggers MECE, changelog_present_and_current — and 5 quality BADGES — unit_tests_present, e2e_tests_present, llm_eval_present ≥3 cases, bootstrap_runbook_present, license_present); tier eligibility: `endorsed` needs all 10, `community` needs core + ≥3 badges, `experimental` needs core only, `blocked` when any core fails. `--quick` ~5s structural sweep; `--fix --yes` auto-scaffolds `auto_fixable: true` dimensions and refuses to overwrite files whose mtime is newer than `skillpack.json`. `modusbrain skillpack init &lt;name&gt;` lands 11 files (skillpack.json, SKILL.md, routing-eval.jsonl, test/example.test.ts, e2e/example.e2e.test.ts, evals/example.judge.json, runbooks/&#123;bootstrap,uninstall,upgrade-template&#125;.md, CHANGELOG, README, LICENSE); freshly-init'd scores 10/10; `--minimal` skips test/e2e/evals. `modusbrain skillpack pack` packs a deterministic tarball via GNU tar (`--sort=name --mtime=@0 --owner=0 --group=0 --numeric-owner` + `GZIP=-n` + `TZ=UTC`); refuses on `tier_eligibility === 'blocked'`. Extract caps (5000 files / 100MB total / 1MB per file / 255-char paths / 100:1 compression ratio); rejects symlinks/hardlinks/devices/FIFOs. `modusbrain skillpack endorse &lt;name&gt; [--tier ...] [--push] [--dry-run]` runs in a clone of the registry repo: validates the pack in `registry.json`, mutates `endorsements.json` via pure `applyEndorsement`, stable-key-orders the write, commits `endorse: &lt;name&gt; -> &lt;tier&gt;`, optionally pushes. JSONL audit at `~/.modusbrain/audit/skillpack-YYYY-Www.jsonl` (ISO-week rotated, honors `MODUSBRAIN_AUDIT_DIR`). `examples/skillpack-reference/` is a 10/10 reference pack pinned by `test/e2e/skillpack-third-party.test.ts`. `docs/skillpack-anatomy.md` auto-generated via `scripts/build-skillpack-anatomy.ts` (`--check` for CI drift). CLI dispatch in `src/commands/skillpack.ts` disambiguates third-party (contains `/`, `://`, `.tgz`) from bundled-skill kebab; kebab routes bundled-first, registry-fallback. Tests: `test/skillpack-&#123;manifest-v1,tarball,state,remote-source,trust-prompt,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit,scaffold-third-party&#125;.test.ts` + `test/e2e/skillpack-third-party.test.ts`. Spec at `docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md`.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L1724) | 1724 | `- `src/commands/extract-conversation-facts.ts` + `src/core/cycle/conversation-facts-backfill.ts` — bulk fact extraction for long-form conversation pages. Walks conversation/meeting/slack/email pages, splits into time-windowed segments (30-min gap or 30-msg cap), prepends a topical/temporal header, and runs through `extractFactsFromTurn()` so anchor-rich facts surface in `modusbrain search`. Invariants: **strict per-source core** (`runExtractConversationFactsCore(&#123;sourceId, ...&#125;)` always takes one sourceId; CLI + cycle phase each do their own multi-source iteration because PHASE_SCOPE='source' is taxonomy-only); **two-phase enumeration** (paginated `listPages(&#123;type, sourceId, limit:10&#125;)`; per-page body cap MAX_PAGE_BODY_BYTES=25MB with `pages_skipped_too_large` counter surfaced in doctor); **page-global row_num accumulator** (facts unique index is `(source_id, source_markdown_slug, row_num)` per migration v51 — per-segment row_num would collide); **page-level TERMINAL audit row** to facts table after all segments commit (source='cli:extract-conversation-facts:terminal'; doctor's NOT EXISTS matches the terminal row so partial-extraction pages stay in backlog); **optional `opts.budgetTracker?`** (when present, used as-is — nested `withBudgetTracker` REPLACES; when absent, core auto-wraps with `BudgetTracker(&#123;maxCostUsd&#125;)`); **body read covers compiled_truth + timeline**; **honors `facts.extraction_enabled` kill-switch** with `--override-disabled` escape; **--types LIST allowlist** (`conversation,meeting,slack,email`) with CLI default reading `cycle.conversation_facts_backfill.types`; **fingerprint on sourceId only**; **string-encoded op-checkpoint** entries `"&lt;sourceId&gt;\|&lt;slug&gt;\|&lt;endIso&gt;"` for resume (durable audit is the facts terminal row); **`--background` via maybeBackground** (Minion handler `extract-conversation-facts` re-creates BudgetTracker from `data.max_cost_usd`; on `BudgetExhausted` mid-job catches + persists + marks `completed` with `result.budget_exhausted=true`). The companion cycle phase `conversation_facts_backfill` (default OFF) iterates `listSources(engine)`, creates ONE brain-wide tracker per tick + wraps the loop in `withBudgetTracker` + passes the tracker into every per-source call. Two-layer cost AND walltime caps: per-source (`max_cost_usd=$1`, `max_walltime_min=20`) AND brain-wide (`max_total_cost_usd=$5`, `max_total_walltime_min=30`). Pinned by `test/extract-conversation-facts.test.ts` (27 cases). Migration v94 adds partial index `idx_facts_extract_conversation_session ON facts(source_id, source_session) WHERE source LIKE 'cli:extract-conversation-facts%'` (`transaction:false` + invalid-index pre-drop on Postgres; plain CREATE INDEX on PGLite). `src/commands/doctor.ts:computeConversationFactsBacklogCheck` is 3-state (SKIPPED when disabled; OK when caught up; WARN when &gt;10 pages lack the terminal row, with paste-ready `modusbrain doctor --remediate` step). `src/commands/sources.ts:runAudit` adds `facts_backfill_estimate: &#123;pages, est_segments, est_cost_usd, types&#125;`. Schema-pack `gbrain-base.yaml` promotes `conversation` (temporal, extractable) + `atom` (annotation, NOT extractable) into the base seed; backstop uses hardcoded `ELIGIBLE_TYPES` in `src/core/facts/eligibility.ts:51` not pack extractable. `ALL_PAGE_TYPES` in `src/core/types.ts` extended with the two new types.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L2190) | 2190 | `- `src/core/skillpack/&#123;manifest-v1,tarball,state,remote-source,trust-prompt,bootstrap-display,scaffold-third-party,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit&#125;.ts` + `examples/skillpack-reference/` + `docs/skillpack-anatomy.md` + `scripts/build-skillpack-anatomy.ts` — third-party skillpack ecosystem. `modusbrain skillpack scaffold &lt;owner/repo\|https-url\|./tgz\|./local-dir&gt;` resolves the spec via `classifySpec`, fetches through SSRF-hardened `git-remote.ts` (git) or extracts the tarball into `~/.modusbrain/skillpack-cache/&lt;host&gt;/&lt;owner&gt;/&lt;repo&gt;/&lt;sha&gt;/`, validates `skillpack.json` (api_version `gbrain-skillpack-v1`), checks `modusbrain_min_version`, surfaces a TOFU first-install identity-confirm prompt (author + source + pinned commit + tarball SHA + tier; non-TTY requires `--trust`), records the pin in machine-owned `~/.modusbrain/skillpack-state.json` (schema `modusbrain-skillpack-state-v1`, atomic `.tmp + rename`, `isAlreadyTrusted` skips re-prompt on author+pin match), runs through `enumerateScaffoldEntries` → `copyArtifacts` (one-time additive, refuses to overwrite), then DISPLAYS `runbooks/bootstrap.md` WITHOUT executing (deliberately does not auto-execute). Registry catalog at `thebuildceo/modusbrain-skillpack-registry` split into `registry.json` (PR-able, `modusbrain-registry-v1`) + `endorsements.json` (Admin-only overlay, `modusbrain-endorsements-v1`); `effectiveTier` merges. `registry-client.ts` fetches both via `If-None-Match` etag with 1h soft-TTL + stale-fallback (origins `fresh_fetch \| cache_warm \| cache_soft_stale \| cache_hard_stale`); hard-fail only on no-cache + no-network. CLI: `modusbrain skillpack &#123;search,info,registry,doctor,init,pack,endorse&#125;`. Doctor walks `SKILLPACK_RUBRIC_V1` (10 binary dimensions: 5 required CORE — manifest_valid, skills_have_skill_md, routing_evals_present ≥5 intents, skills_have_unique_triggers MECE, changelog_present_and_current — and 5 quality BADGES — unit_tests_present, e2e_tests_present, llm_eval_present ≥3 cases, bootstrap_runbook_present, license_present); tier eligibility: `endorsed` needs all 10, `community` needs core + ≥3 badges, `experimental` needs core only, `blocked` when any core fails. `--quick` ~5s structural sweep; `--fix --yes` auto-scaffolds `auto_fixable: true` dimensions and refuses to overwrite files whose mtime is newer than `skillpack.json`. `modusbrain skillpack init &lt;name&gt;` lands 11 files (skillpack.json, SKILL.md, routing-eval.jsonl, test/example.test.ts, e2e/example.e2e.test.ts, evals/example.judge.json, runbooks/&#123;bootstrap,uninstall,upgrade-template&#125;.md, CHANGELOG, README, LICENSE); freshly-init'd scores 10/10; `--minimal` skips test/e2e/evals. `modusbrain skillpack pack` packs a deterministic tarball via GNU tar (`--sort=name --mtime=@0 --owner=0 --group=0 --numeric-owner` + `GZIP=-n` + `TZ=UTC`); refuses on `tier_eligibility === 'blocked'`. Extract caps (5000 files / 100MB total / 1MB per file / 255-char paths / 100:1 compression ratio); rejects symlinks/hardlinks/devices/FIFOs. `modusbrain skillpack endorse &lt;name&gt; [--tier ...] [--push] [--dry-run]` runs in a clone of the registry repo: validates the pack in `registry.json`, mutates `endorsements.json` via pure `applyEndorsement`, stable-key-orders the write, commits `endorse: &lt;name&gt; -> &lt;tier&gt;`, optionally pushes. JSONL audit at `~/.modusbrain/audit/skillpack-YYYY-Www.jsonl` (ISO-week rotated, honors `MODUSBRAIN_AUDIT_DIR`). `examples/skillpack-reference/` is a 10/10 reference pack pinned by `test/e2e/skillpack-third-party.test.ts`. `docs/skillpack-anatomy.md` auto-generated via `scripts/build-skillpack-anatomy.ts` (`--check` for CI drift). CLI dispatch in `src/commands/skillpack.ts` disambiguates third-party (contains `/`, `://`, `.tgz`) from bundled-skill kebab; kebab routes bundled-first, registry-fallback. Tests: `test/skillpack-&#123;manifest-v1,tarball,state,remote-source,trust-prompt,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit,scaffold-third-party&#125;.test.ts` + `test/e2e/skillpack-third-party.test.ts`. Spec at `docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md`.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L2232) | 2232 | `- `src/commands/extract-conversation-facts.ts` + `src/core/cycle/conversation-facts-backfill.ts` — bulk fact extraction for long-form conversation pages. Walks conversation/meeting/slack/email pages, splits into time-windowed segments (30-min gap or 30-msg cap), prepends a topical/temporal header, and runs through `extractFactsFromTurn()` so anchor-rich facts surface in `modusbrain search`. Invariants: **strict per-source core** (`runExtractConversationFactsCore(&#123;sourceId, ...&#125;)` always takes one sourceId; CLI + cycle phase each do their own multi-source iteration because PHASE_SCOPE='source' is taxonomy-only); **two-phase enumeration** (paginated `listPages(&#123;type, sourceId, limit:10&#125;)`; per-page body cap MAX_PAGE_BODY_BYTES=25MB with `pages_skipped_too_large` counter surfaced in doctor); **page-global row_num accumulator** (facts unique index is `(source_id, source_markdown_slug, row_num)` per migration v51 — per-segment row_num would collide); **page-level TERMINAL audit row** to facts table after all segments commit (source='cli:extract-conversation-facts:terminal'; doctor's NOT EXISTS matches the terminal row so partial-extraction pages stay in backlog); **optional `opts.budgetTracker?`** (when present, used as-is — nested `withBudgetTracker` REPLACES; when absent, core auto-wraps with `BudgetTracker(&#123;maxCostUsd&#125;)`); **body read covers compiled_truth + timeline**; **honors `facts.extraction_enabled` kill-switch** with `--override-disabled` escape; **--types LIST allowlist** (`conversation,meeting,slack,email`) with CLI default reading `cycle.conversation_facts_backfill.types`; **fingerprint on sourceId only**; **string-encoded op-checkpoint** entries `"&lt;sourceId&gt;\|&lt;slug&gt;\|&lt;endIso&gt;"` for resume (durable audit is the facts terminal row); **`--background` via maybeBackground** (Minion handler `extract-conversation-facts` re-creates BudgetTracker from `data.max_cost_usd`; on `BudgetExhausted` mid-job catches + persists + marks `completed` with `result.budget_exhausted=true`). The companion cycle phase `conversation_facts_backfill` (default OFF) iterates `listSources(engine)`, creates ONE brain-wide tracker per tick + wraps the loop in `withBudgetTracker` + passes the tracker into every per-source call. Two-layer cost AND walltime caps: per-source (`max_cost_usd=$1`, `max_walltime_min=20`) AND brain-wide (`max_total_cost_usd=$5`, `max_total_walltime_min=30`). Pinned by `test/extract-conversation-facts.test.ts` (27 cases). Migration v94 adds partial index `idx_facts_extract_conversation_session ON facts(source_id, source_session) WHERE source LIKE 'cli:extract-conversation-facts%'` (`transaction:false` + invalid-index pre-drop on Postgres; plain CREATE INDEX on PGLite). `src/commands/doctor.ts:computeConversationFactsBacklogCheck` is 3-state (SKIPPED when disabled; OK when caught up; WARN when &gt;10 pages lack the terminal row, with paste-ready `modusbrain doctor --remediate` step). `src/commands/sources.ts:runAudit` adds `facts_backfill_estimate: &#123;pages, est_segments, est_cost_usd, types&#125;`. Schema-pack `gbrain-base.yaml` promotes `conversation` (temporal, extractable) + `atom` (annotation, NOT extractable) into the base seed; backstop uses hardcoded `ELIGIBLE_TYPES` in `src/core/facts/eligibility.ts:51` not pack extractable. `ALL_PAGE_TYPES` in `src/core/types.ts` extended with the two new types.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L2598) | 2598 | `- `src/core/skillpack/&#123;manifest-v1,tarball,state,remote-source,trust-prompt,bootstrap-display,scaffold-third-party,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit&#125;.ts` + `examples/skillpack-reference/` + `docs/skillpack-anatomy.md` + `scripts/build-skillpack-anatomy.ts` — third-party skillpack ecosystem. `modusbrain skillpack scaffold &lt;owner/repo\|https-url\|./tgz\|./local-dir&gt;` resolves the spec via `classifySpec`, fetches through SSRF-hardened `git-remote.ts` (git) or extracts the tarball into `~/.modusbrain/skillpack-cache/&lt;host&gt;/&lt;owner&gt;/&lt;repo&gt;/&lt;sha&gt;/`, validates `skillpack.json` (api_version `gbrain-skillpack-v1`), checks `modusbrain_min_version`, surfaces a TOFU first-install identity-confirm prompt (author + source + pinned commit + tarball SHA + tier; non-TTY requires `--trust`), records the pin in machine-owned `~/.modusbrain/skillpack-state.json` (schema `modusbrain-skillpack-state-v1`, atomic `.tmp + rename`, `isAlreadyTrusted` skips re-prompt on author+pin match), runs through `enumerateScaffoldEntries` → `copyArtifacts` (one-time additive, refuses to overwrite), then DISPLAYS `runbooks/bootstrap.md` WITHOUT executing (deliberately does not auto-execute). Registry catalog at `thebuildceo/modusbrain-skillpack-registry` split into `registry.json` (PR-able, `modusbrain-registry-v1`) + `endorsements.json` (Admin-only overlay, `modusbrain-endorsements-v1`); `effectiveTier` merges. `registry-client.ts` fetches both via `If-None-Match` etag with 1h soft-TTL + stale-fallback (origins `fresh_fetch \| cache_warm \| cache_soft_stale \| cache_hard_stale`); hard-fail only on no-cache + no-network. CLI: `modusbrain skillpack &#123;search,info,registry,doctor,init,pack,endorse&#125;`. Doctor walks `SKILLPACK_RUBRIC_V1` (10 binary dimensions: 5 required CORE — manifest_valid, skills_have_skill_md, routing_evals_present ≥5 intents, skills_have_unique_triggers MECE, changelog_present_and_current — and 5 quality BADGES — unit_tests_present, e2e_tests_present, llm_eval_present ≥3 cases, bootstrap_runbook_present, license_present); tier eligibility: `endorsed` needs all 10, `community` needs core + ≥3 badges, `experimental` needs core only, `blocked` when any core fails. `--quick` ~5s structural sweep; `--fix --yes` auto-scaffolds `auto_fixable: true` dimensions and refuses to overwrite files whose mtime is newer than `skillpack.json`. `modusbrain skillpack init &lt;name&gt;` lands 11 files (skillpack.json, SKILL.md, routing-eval.jsonl, test/example.test.ts, e2e/example.e2e.test.ts, evals/example.judge.json, runbooks/&#123;bootstrap,uninstall,upgrade-template&#125;.md, CHANGELOG, README, LICENSE); freshly-init'd scores 10/10; `--minimal` skips test/e2e/evals. `modusbrain skillpack pack` packs a deterministic tarball via GNU tar (`--sort=name --mtime=@0 --owner=0 --group=0 --numeric-owner` + `GZIP=-n` + `TZ=UTC`); refuses on `tier_eligibility === 'blocked'`. Extract caps (5000 files / 100MB total / 1MB per file / 255-char paths / 100:1 compression ratio); rejects symlinks/hardlinks/devices/FIFOs. `modusbrain skillpack endorse &lt;name&gt; [--tier ...] [--push] [--dry-run]` runs in a clone of the registry repo: validates the pack in `registry.json`, mutates `endorsements.json` via pure `applyEndorsement`, stable-key-orders the write, commits `endorse: &lt;name&gt; -> &lt;tier&gt;`, optionally pushes. JSONL audit at `~/.modusbrain/audit/skillpack-YYYY-Www.jsonl` (ISO-week rotated, honors `MODUSBRAIN_AUDIT_DIR`). `examples/skillpack-reference/` is a 10/10 reference pack pinned by `test/e2e/skillpack-third-party.test.ts`. `docs/skillpack-anatomy.md` auto-generated via `scripts/build-skillpack-anatomy.ts` (`--check` for CI drift). CLI dispatch in `src/commands/skillpack.ts` disambiguates third-party (contains `/`, `://`, `.tgz`) from bundled-skill kebab; kebab routes bundled-first, registry-fallback. Tests: `test/skillpack-&#123;manifest-v1,tarball,state,remote-source,trust-prompt,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit,scaffold-third-party&#125;.test.ts` + `test/e2e/skillpack-third-party.test.ts`. Spec at `docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md`.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L2640) | 2640 | `- `src/commands/extract-conversation-facts.ts` + `src/core/cycle/conversation-facts-backfill.ts` — bulk fact extraction for long-form conversation pages. Walks conversation/meeting/slack/email pages, splits into time-windowed segments (30-min gap or 30-msg cap), prepends a topical/temporal header, and runs through `extractFactsFromTurn()` so anchor-rich facts surface in `modusbrain search`. Invariants: **strict per-source core** (`runExtractConversationFactsCore(&#123;sourceId, ...&#125;)` always takes one sourceId; CLI + cycle phase each do their own multi-source iteration because PHASE_SCOPE='source' is taxonomy-only); **two-phase enumeration** (paginated `listPages(&#123;type, sourceId, limit:10&#125;)`; per-page body cap MAX_PAGE_BODY_BYTES=25MB with `pages_skipped_too_large` counter surfaced in doctor); **page-global row_num accumulator** (facts unique index is `(source_id, source_markdown_slug, row_num)` per migration v51 — per-segment row_num would collide); **page-level TERMINAL audit row** to facts table after all segments commit (source='cli:extract-conversation-facts:terminal'; doctor's NOT EXISTS matches the terminal row so partial-extraction pages stay in backlog); **optional `opts.budgetTracker?`** (when present, used as-is — nested `withBudgetTracker` REPLACES; when absent, core auto-wraps with `BudgetTracker(&#123;maxCostUsd&#125;)`); **body read covers compiled_truth + timeline**; **honors `facts.extraction_enabled` kill-switch** with `--override-disabled` escape; **--types LIST allowlist** (`conversation,meeting,slack,email`) with CLI default reading `cycle.conversation_facts_backfill.types`; **fingerprint on sourceId only**; **string-encoded op-checkpoint** entries `"&lt;sourceId&gt;\|&lt;slug&gt;\|&lt;endIso&gt;"` for resume (durable audit is the facts terminal row); **`--background` via maybeBackground** (Minion handler `extract-conversation-facts` re-creates BudgetTracker from `data.max_cost_usd`; on `BudgetExhausted` mid-job catches + persists + marks `completed` with `result.budget_exhausted=true`). The companion cycle phase `conversation_facts_backfill` (default OFF) iterates `listSources(engine)`, creates ONE brain-wide tracker per tick + wraps the loop in `withBudgetTracker` + passes the tracker into every per-source call. Two-layer cost AND walltime caps: per-source (`max_cost_usd=$1`, `max_walltime_min=20`) AND brain-wide (`max_total_cost_usd=$5`, `max_total_walltime_min=30`). Pinned by `test/extract-conversation-facts.test.ts` (27 cases). Migration v94 adds partial index `idx_facts_extract_conversation_session ON facts(source_id, source_session) WHERE source LIKE 'cli:extract-conversation-facts%'` (`transaction:false` + invalid-index pre-drop on Postgres; plain CREATE INDEX on PGLite). `src/commands/doctor.ts:computeConversationFactsBacklogCheck` is 3-state (SKIPPED when disabled; OK when caught up; WARN when &gt;10 pages lack the terminal row, with paste-ready `modusbrain doctor --remediate` step). `src/commands/sources.ts:runAudit` adds `facts_backfill_estimate: &#123;pages, est_segments, est_cost_usd, types&#125;`. Schema-pack `gbrain-base.yaml` promotes `conversation` (temporal, extractable) + `atom` (annotation, NOT extractable) into the base seed; backstop uses hardcoded `ELIGIBLE_TYPES` in `src/core/facts/eligibility.ts:51` not pack extractable. `ALL_PAGE_TYPES` in `src/core/types.ts` extended with the two new types.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L3098) | 3098 | `- `src/core/skillpack/&#123;manifest-v1,tarball,state,remote-source,trust-prompt,bootstrap-display,scaffold-third-party,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit&#125;.ts` + `examples/skillpack-reference/` + `docs/skillpack-anatomy.md` + `scripts/build-skillpack-anatomy.ts` — third-party skillpack ecosystem. `modusbrain skillpack scaffold &lt;owner/repo\|https-url\|./tgz\|./local-dir&gt;` resolves the spec via `classifySpec`, fetches through SSRF-hardened `git-remote.ts` (git) or extracts the tarball into `~/.modusbrain/skillpack-cache/&lt;host&gt;/&lt;owner&gt;/&lt;repo&gt;/&lt;sha&gt;/`, validates `skillpack.json` (api_version `gbrain-skillpack-v1`), checks `modusbrain_min_version`, surfaces a TOFU first-install identity-confirm prompt (author + source + pinned commit + tarball SHA + tier; non-TTY requires `--trust`), records the pin in machine-owned `~/.modusbrain/skillpack-state.json` (schema `modusbrain-skillpack-state-v1`, atomic `.tmp + rename`, `isAlreadyTrusted` skips re-prompt on author+pin match), runs through `enumerateScaffoldEntries` → `copyArtifacts` (one-time additive, refuses to overwrite), then DISPLAYS `runbooks/bootstrap.md` WITHOUT executing (deliberately does not auto-execute). Registry catalog at `thebuildceo/modusbrain-skillpack-registry` split into `registry.json` (PR-able, `modusbrain-registry-v1`) + `endorsements.json` (Admin-only overlay, `modusbrain-endorsements-v1`); `effectiveTier` merges. `registry-client.ts` fetches both via `If-None-Match` etag with 1h soft-TTL + stale-fallback (origins `fresh_fetch \| cache_warm \| cache_soft_stale \| cache_hard_stale`); hard-fail only on no-cache + no-network. CLI: `modusbrain skillpack &#123;search,info,registry,doctor,init,pack,endorse&#125;`. Doctor walks `SKILLPACK_RUBRIC_V1` (10 binary dimensions: 5 required CORE — manifest_valid, skills_have_skill_md, routing_evals_present ≥5 intents, skills_have_unique_triggers MECE, changelog_present_and_current — and 5 quality BADGES — unit_tests_present, e2e_tests_present, llm_eval_present ≥3 cases, bootstrap_runbook_present, license_present); tier eligibility: `endorsed` needs all 10, `community` needs core + ≥3 badges, `experimental` needs core only, `blocked` when any core fails. `--quick` ~5s structural sweep; `--fix --yes` auto-scaffolds `auto_fixable: true` dimensions and refuses to overwrite files whose mtime is newer than `skillpack.json`. `modusbrain skillpack init &lt;name&gt;` lands 11 files (skillpack.json, SKILL.md, routing-eval.jsonl, test/example.test.ts, e2e/example.e2e.test.ts, evals/example.judge.json, runbooks/&#123;bootstrap,uninstall,upgrade-template&#125;.md, CHANGELOG, README, LICENSE); freshly-init'd scores 10/10; `--minimal` skips test/e2e/evals. `modusbrain skillpack pack` packs a deterministic tarball via GNU tar (`--sort=name --mtime=@0 --owner=0 --group=0 --numeric-owner` + `GZIP=-n` + `TZ=UTC`); refuses on `tier_eligibility === 'blocked'`. Extract caps (5000 files / 100MB total / 1MB per file / 255-char paths / 100:1 compression ratio); rejects symlinks/hardlinks/devices/FIFOs. `modusbrain skillpack endorse &lt;name&gt; [--tier ...] [--push] [--dry-run]` runs in a clone of the registry repo: validates the pack in `registry.json`, mutates `endorsements.json` via pure `applyEndorsement`, stable-key-orders the write, commits `endorse: &lt;name&gt; -> &lt;tier&gt;`, optionally pushes. JSONL audit at `~/.modusbrain/audit/skillpack-YYYY-Www.jsonl` (ISO-week rotated, honors `MODUSBRAIN_AUDIT_DIR`). `examples/skillpack-reference/` is a 10/10 reference pack pinned by `test/e2e/skillpack-third-party.test.ts`. `docs/skillpack-anatomy.md` auto-generated via `scripts/build-skillpack-anatomy.ts` (`--check` for CI drift). CLI dispatch in `src/commands/skillpack.ts` disambiguates third-party (contains `/`, `://`, `.tgz`) from bundled-skill kebab; kebab routes bundled-first, registry-fallback. Tests: `test/skillpack-&#123;manifest-v1,tarball,state,remote-source,trust-prompt,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit,scaffold-third-party&#125;.test.ts` + `test/e2e/skillpack-third-party.test.ts`. Spec at `docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md`.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L3140) | 3140 | `- `src/commands/extract-conversation-facts.ts` + `src/core/cycle/conversation-facts-backfill.ts` — bulk fact extraction for long-form conversation pages. Walks conversation/meeting/slack/email pages, splits into time-windowed segments (30-min gap or 30-msg cap), prepends a topical/temporal header, and runs through `extractFactsFromTurn()` so anchor-rich facts surface in `modusbrain search`. Invariants: **strict per-source core** (`runExtractConversationFactsCore(&#123;sourceId, ...&#125;)` always takes one sourceId; CLI + cycle phase each do their own multi-source iteration because PHASE_SCOPE='source' is taxonomy-only); **two-phase enumeration** (paginated `listPages(&#123;type, sourceId, limit:10&#125;)`; per-page body cap MAX_PAGE_BODY_BYTES=25MB with `pages_skipped_too_large` counter surfaced in doctor); **page-global row_num accumulator** (facts unique index is `(source_id, source_markdown_slug, row_num)` per migration v51 — per-segment row_num would collide); **page-level TERMINAL audit row** to facts table after all segments commit (source='cli:extract-conversation-facts:terminal'; doctor's NOT EXISTS matches the terminal row so partial-extraction pages stay in backlog); **optional `opts.budgetTracker?`** (when present, used as-is — nested `withBudgetTracker` REPLACES; when absent, core auto-wraps with `BudgetTracker(&#123;maxCostUsd&#125;)`); **body read covers compiled_truth + timeline**; **honors `facts.extraction_enabled` kill-switch** with `--override-disabled` escape; **--types LIST allowlist** (`conversation,meeting,slack,email`) with CLI default reading `cycle.conversation_facts_backfill.types`; **fingerprint on sourceId only**; **string-encoded op-checkpoint** entries `"&lt;sourceId&gt;\|&lt;slug&gt;\|&lt;endIso&gt;"` for resume (durable audit is the facts terminal row); **`--background` via maybeBackground** (Minion handler `extract-conversation-facts` re-creates BudgetTracker from `data.max_cost_usd`; on `BudgetExhausted` mid-job catches + persists + marks `completed` with `result.budget_exhausted=true`). The companion cycle phase `conversation_facts_backfill` (default OFF) iterates `listSources(engine)`, creates ONE brain-wide tracker per tick + wraps the loop in `withBudgetTracker` + passes the tracker into every per-source call. Two-layer cost AND walltime caps: per-source (`max_cost_usd=$1`, `max_walltime_min=20`) AND brain-wide (`max_total_cost_usd=$5`, `max_total_walltime_min=30`). Pinned by `test/extract-conversation-facts.test.ts` (27 cases). Migration v94 adds partial index `idx_facts_extract_conversation_session ON facts(source_id, source_session) WHERE source LIKE 'cli:extract-conversation-facts%'` (`transaction:false` + invalid-index pre-drop on Postgres; plain CREATE INDEX on PGLite). `src/commands/doctor.ts:computeConversationFactsBacklogCheck` is 3-state (SKIPPED when disabled; OK when caught up; WARN when &gt;10 pages lack the terminal row, with paste-ready `modusbrain doctor --remediate` step). `src/commands/sources.ts:runAudit` adds `facts_backfill_estimate: &#123;pages, est_segments, est_cost_usd, types&#125;`. Schema-pack `gbrain-base.yaml` promotes `conversation` (temporal, extractable) + `atom` (annotation, NOT extractable) into the base seed; backstop uses hardcoded `ELIGIBLE_TYPES` in `src/core/facts/eligibility.ts:51` not pack extractable. `ALL_PAGE_TYPES` in `src/core/types.ts` extended with the two new types.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L3506) | 3506 | `- `src/core/skillpack/&#123;manifest-v1,tarball,state,remote-source,trust-prompt,bootstrap-display,scaffold-third-party,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit&#125;.ts` + `examples/skillpack-reference/` + `docs/skillpack-anatomy.md` + `scripts/build-skillpack-anatomy.ts` — third-party skillpack ecosystem. `modusbrain skillpack scaffold &lt;owner/repo\|https-url\|./tgz\|./local-dir&gt;` resolves the spec via `classifySpec`, fetches through SSRF-hardened `git-remote.ts` (git) or extracts the tarball into `~/.modusbrain/skillpack-cache/&lt;host&gt;/&lt;owner&gt;/&lt;repo&gt;/&lt;sha&gt;/`, validates `skillpack.json` (api_version `gbrain-skillpack-v1`), checks `modusbrain_min_version`, surfaces a TOFU first-install identity-confirm prompt (author + source + pinned commit + tarball SHA + tier; non-TTY requires `--trust`), records the pin in machine-owned `~/.modusbrain/skillpack-state.json` (schema `modusbrain-skillpack-state-v1`, atomic `.tmp + rename`, `isAlreadyTrusted` skips re-prompt on author+pin match), runs through `enumerateScaffoldEntries` → `copyArtifacts` (one-time additive, refuses to overwrite), then DISPLAYS `runbooks/bootstrap.md` WITHOUT executing (deliberately does not auto-execute). Registry catalog at `thebuildceo/modusbrain-skillpack-registry` split into `registry.json` (PR-able, `modusbrain-registry-v1`) + `endorsements.json` (Admin-only overlay, `modusbrain-endorsements-v1`); `effectiveTier` merges. `registry-client.ts` fetches both via `If-None-Match` etag with 1h soft-TTL + stale-fallback (origins `fresh_fetch \| cache_warm \| cache_soft_stale \| cache_hard_stale`); hard-fail only on no-cache + no-network. CLI: `modusbrain skillpack &#123;search,info,registry,doctor,init,pack,endorse&#125;`. Doctor walks `SKILLPACK_RUBRIC_V1` (10 binary dimensions: 5 required CORE — manifest_valid, skills_have_skill_md, routing_evals_present ≥5 intents, skills_have_unique_triggers MECE, changelog_present_and_current — and 5 quality BADGES — unit_tests_present, e2e_tests_present, llm_eval_present ≥3 cases, bootstrap_runbook_present, license_present); tier eligibility: `endorsed` needs all 10, `community` needs core + ≥3 badges, `experimental` needs core only, `blocked` when any core fails. `--quick` ~5s structural sweep; `--fix --yes` auto-scaffolds `auto_fixable: true` dimensions and refuses to overwrite files whose mtime is newer than `skillpack.json`. `modusbrain skillpack init &lt;name&gt;` lands 11 files (skillpack.json, SKILL.md, routing-eval.jsonl, test/example.test.ts, e2e/example.e2e.test.ts, evals/example.judge.json, runbooks/&#123;bootstrap,uninstall,upgrade-template&#125;.md, CHANGELOG, README, LICENSE); freshly-init'd scores 10/10; `--minimal` skips test/e2e/evals. `modusbrain skillpack pack` packs a deterministic tarball via GNU tar (`--sort=name --mtime=@0 --owner=0 --group=0 --numeric-owner` + `GZIP=-n` + `TZ=UTC`); refuses on `tier_eligibility === 'blocked'`. Extract caps (5000 files / 100MB total / 1MB per file / 255-char paths / 100:1 compression ratio); rejects symlinks/hardlinks/devices/FIFOs. `modusbrain skillpack endorse &lt;name&gt; [--tier ...] [--push] [--dry-run]` runs in a clone of the registry repo: validates the pack in `registry.json`, mutates `endorsements.json` via pure `applyEndorsement`, stable-key-orders the write, commits `endorse: &lt;name&gt; -> &lt;tier&gt;`, optionally pushes. JSONL audit at `~/.modusbrain/audit/skillpack-YYYY-Www.jsonl` (ISO-week rotated, honors `MODUSBRAIN_AUDIT_DIR`). `examples/skillpack-reference/` is a 10/10 reference pack pinned by `test/e2e/skillpack-third-party.test.ts`. `docs/skillpack-anatomy.md` auto-generated via `scripts/build-skillpack-anatomy.ts` (`--check` for CI drift). CLI dispatch in `src/commands/skillpack.ts` disambiguates third-party (contains `/`, `://`, `.tgz`) from bundled-skill kebab; kebab routes bundled-first, registry-fallback. Tests: `test/skillpack-&#123;manifest-v1,tarball,state,remote-source,trust-prompt,registry-schema,registry-client,rubric,doctor,init-scaffold,pack-publish,endorse,audit,scaffold-third-party&#125;.test.ts` + `test/e2e/skillpack-third-party.test.ts`. Spec at `docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md`.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L3548) | 3548 | `- `src/commands/extract-conversation-facts.ts` + `src/core/cycle/conversation-facts-backfill.ts` — bulk fact extraction for long-form conversation pages. Walks conversation/meeting/slack/email pages, splits into time-windowed segments (30-min gap or 30-msg cap), prepends a topical/temporal header, and runs through `extractFactsFromTurn()` so anchor-rich facts surface in `modusbrain search`. Invariants: **strict per-source core** (`runExtractConversationFactsCore(&#123;sourceId, ...&#125;)` always takes one sourceId; CLI + cycle phase each do their own multi-source iteration because PHASE_SCOPE='source' is taxonomy-only); **two-phase enumeration** (paginated `listPages(&#123;type, sourceId, limit:10&#125;)`; per-page body cap MAX_PAGE_BODY_BYTES=25MB with `pages_skipped_too_large` counter surfaced in doctor); **page-global row_num accumulator** (facts unique index is `(source_id, source_markdown_slug, row_num)` per migration v51 — per-segment row_num would collide); **page-level TERMINAL audit row** to facts table after all segments commit (source='cli:extract-conversation-facts:terminal'; doctor's NOT EXISTS matches the terminal row so partial-extraction pages stay in backlog); **optional `opts.budgetTracker?`** (when present, used as-is — nested `withBudgetTracker` REPLACES; when absent, core auto-wraps with `BudgetTracker(&#123;maxCostUsd&#125;)`); **body read covers compiled_truth + timeline**; **honors `facts.extraction_enabled` kill-switch** with `--override-disabled` escape; **--types LIST allowlist** (`conversation,meeting,slack,email`) with CLI default reading `cycle.conversation_facts_backfill.types`; **fingerprint on sourceId only**; **string-encoded op-checkpoint** entries `"&lt;sourceId&gt;\|&lt;slug&gt;\|&lt;endIso&gt;"` for resume (durable audit is the facts terminal row); **`--background` via maybeBackground** (Minion handler `extract-conversation-facts` re-creates BudgetTracker from `data.max_cost_usd`; on `BudgetExhausted` mid-job catches + persists + marks `completed` with `result.budget_exhausted=true`). The companion cycle phase `conversation_facts_backfill` (default OFF) iterates `listSources(engine)`, creates ONE brain-wide tracker per tick + wraps the loop in `withBudgetTracker` + passes the tracker into every per-source call. Two-layer cost AND walltime caps: per-source (`max_cost_usd=$1`, `max_walltime_min=20`) AND brain-wide (`max_total_cost_usd=$5`, `max_total_walltime_min=30`). Pinned by `test/extract-conversation-facts.test.ts` (27 cases). Migration v94 adds partial index `idx_facts_extract_conversation_session ON facts(source_id, source_session) WHERE source LIKE 'cli:extract-conversation-facts%'` (`transaction:false` + invalid-index pre-drop on Postgres; plain CREATE INDEX on PGLite). `src/commands/doctor.ts:computeConversationFactsBacklogCheck` is 3-state (SKIPPED when disabled; OK when caught up; WARN when &gt;10 pages lack the terminal row, with paste-ready `modusbrain doctor --remediate` step). `src/commands/sources.ts:runAudit` adds `facts_backfill_estimate: &#123;pages, est_segments, est_cost_usd, types&#125;`. Schema-pack `gbrain-base.yaml` promotes `conversation` (temporal, extractable) + `atom` (annotation, NOT extractable) into the base seed; backstop uses hardcoded `ELIGIBLE_TYPES` in `src/core/facts/eligibility.ts:51` not pack extractable. `ALL_PAGE_TYPES` in `src/core/types.ts` extended with the two new types.` | GBrain |
| [docs/architecture/KEY_FILES.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/KEY_FILES.md#L3674) | 3674 | `- `src/core/cycle/emotional-weight.ts` — Pure function `computeEmotionalWeight(&#123;tags, takes&#125;, &#123;highEmotionTags?, userHolder?&#125;)`. Deterministic 0..1 score: tag-emotion boost (max 0.5, case-insensitive match against `HIGH_EMOTION_TAGS` seed list), take density (0.1/take, capped at 0.3), take avg weight (0..0.1), user-holder ratio (0..0.1 over active takes; default holder `'garry'`). Total clamped to [0..1]. Anglocentric / personal-life-biased seed list intentional; override via config `emotional_weight.high_tags` (JSON array). `userHolder` overridable via `emotional_weight.user_holder`.` | Garry/Gary |
| [docs/architecture/lens-packs.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/lens-packs.md#L16) | 16 | `gbrain-base (shipped v0.38)` | GBrain |
| [docs/architecture/lens-packs.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/lens-packs.md#L50) | 50 | `YC / investor lens. Declares 2 net-new page types on top of` | YC/Y Combinator |
| [docs/architecture/lens-packs.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/lens-packs.md#L51) | 51 | `gbrain-base's deal/person/company/yc seed:` | YC/Y Combinator, GBrain |
| [docs/architecture/pack-upgrade-mechanism.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/pack-upgrade-mechanism.md#L9) | 9 | `> How `gbrain-base@1.x → gbrain-base-v2@1.0.0` (and any future pack` | GBrain |
| [docs/architecture/pack-upgrade-mechanism.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/pack-upgrade-mechanism.md#L18) | 18 | `name: gbrain-base-v2` | GBrain |
| [docs/architecture/pack-upgrade-mechanism.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/pack-upgrade-mechanism.md#L21) | 21 | `pack: gbrain-base` | GBrain |
| [docs/architecture/pack-upgrade-mechanism.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/pack-upgrade-mechanism.md#L65) | 65 | `│      --params '{"target_pack":"gbrain-base-v2"}'               │` | GBrain |
| [docs/architecture/pack-upgrade-mechanism.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/pack-upgrade-mechanism.md#L126) | 126 | `Walks `BUNDLED_PACK_NAMES` (currently `gbrain-base`,` | GBrain |
| [docs/architecture/pack-upgrade-mechanism.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/pack-upgrade-mechanism.md#L128) | 128 | ``modusbrain-engineer`, `modusbrain-everything`, `gbrain-base-v2`). For each` | GBrain |
| [docs/architecture/pack-upgrade-mechanism.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/pack-upgrade-mechanism.md#L177) | 177 | `pack: gbrain-base-v2` | GBrain |
| [docs/architecture/pack-upgrade-mechanism.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/pack-upgrade-mechanism.md#L181) | 181 | `# Inherit gbrain-base-v2's 15 types here (or use extends to merge` | GBrain |
| [docs/architecture/pack-upgrade-mechanism.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/pack-upgrade-mechanism.md#L212) | 212 | ``gbrain-base-v2@1.x` and surfaces a `unify-types` RemediationStep` | GBrain |
| [docs/architecture/pack-upgrade-mechanism.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/pack-upgrade-mechanism.md#L244) | 244 | `- Pack file: `src/core/schema-pack/base/gbrain-base-v2.yaml`` | GBrain |
| [docs/architecture/RETRIEVAL.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/RETRIEVAL.md#L13) | 13 | `1. **Vector (HNSW on pgvector)** — semantic similarity. Catches "who works on retrieval quality?" → pages mentioning "Shubham Chavan + retrieval" even when the user never typed "YC".` | YC/Y Combinator |
| [docs/architecture/schema-packs.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/schema-packs.md#L25) | 25 | `- **`gbrain-base`** (default) — reproduces pre-v0.38 hardcoded behavior` | GBrain |
| [docs/architecture/schema-packs.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/schema-packs.md#L31) | 31 | `- **`modusbrain-recommended`** — extends `gbrain-base` with the 13 additional` | GBrain |
| [docs/architecture/schema-packs.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/schema-packs.md#L91) | 91 | `\| 7 \| Default: `gbrain-base` \| Always present. \|` | GBrain |
| [docs/architecture/schema-packs.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/schema-packs.md#L113) | 113 | `brain looks unfamiliar — the default `gbrain-base` pack expects` | GBrain |
| [docs/architecture/schema-packs.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/schema-packs.md#L154) | 154 | `extends: gbrain-base   # inherits everything from base; add overrides below` | GBrain |
| [docs/architecture/schema-packs.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/schema-packs.md#L186) | 186 | `modusbrain schema downgrade --to gbrain-base` | GBrain |
| [docs/architecture/schema-packs.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/schema-packs.md#L194) | 194 | `2. `modusbrain schema downgrade --to gbrain-base` — restores config.` | GBrain |
| [docs/architecture/schema-packs.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/schema-packs.md#L210) | 210 | `- `gbrain-skillpack-v1` → skillpack` | GBrain |
| [docs/architecture/type-taxonomy.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/type-taxonomy.md#L7) | 7 | `# Type Taxonomy (v0.41.22: gbrain-base-v2)` | GBrain |
| [docs/architecture/type-taxonomy.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/type-taxonomy.md#L10) | 10 | `> `gbrain-base` (24 types) stays bundled for back-compat; v0.42+ installs` | GBrain |
| [docs/architecture/type-taxonomy.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/type-taxonomy.md#L11) | 11 | `> default to `gbrain-base-v2`.` | GBrain |
| [docs/architecture/type-taxonomy.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/type-taxonomy.md#L42) | 42 | `\| `company` \| entity \| Companies, products, orgs (subtype-distinguished) \| Companies, YC-companies, products \|` | YC/Y Combinator |
| [docs/architecture/type-taxonomy.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/type-taxonomy.md#L85) | 85 | `--params '{"target_pack":"gbrain-base-v2"}'` | GBrain |
| [docs/architecture/type-taxonomy.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/type-taxonomy.md#L119) | 119 | `\| Active-pack flip \| `modusbrain schema use gbrain-base` reverses the flip. \|` | GBrain |
| [docs/architecture/type-taxonomy.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/type-taxonomy.md#L132) | 132 | `1. Fork gbrain-base-v2: `modusbrain schema fork gbrain-base-v2 my-pack`` | GBrain |
| [docs/architecture/type-taxonomy.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/type-taxonomy.md#L138) | 138 | `Your fork can also declare `migration_from: {pack: gbrain-base-v2,` | GBrain |
| [docs/architecture/type-taxonomy.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/architecture/type-taxonomy.md#L178) | 178 | `- Pack file: `src/core/schema-pack/base/gbrain-base-v2.yaml`` | GBrain |
| [docs/designs/KNOWLEDGE_RUNTIME.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/KNOWLEDGE_RUNTIME.md#L467) | 467 | `// "[Source: [X/garrytan, 2026-04-18](https://x.com/shubham/status/123456)]"` | Garry/Gary, garrytan |
| [docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md#L163) | 163 | `"tags": ["evaluation", "yc", "founders"],` | YC/Y Combinator |
| [docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md#L215) | 215 | `Author:        garrytan` | Garry/Gary, garrytan |
| [docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md#L378) | 378 | `"api_version": "gbrain-skillpack-v1",` | GBrain |
| [docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md#L382) | 382 | `"author": "Shubham Chavan <garry@ycombinator.com>",` | Garry/Gary, YC/Y Combinator |
| [docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md#L406) | 406 | `Schema is `gbrain-skillpack-v1`. Codex outside-voice gap: single` | GBrain |
| [docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md#L502) | 502 | `- On yes: `modusbrain put_page wiki/concepts/yc-rubric < seeds/rubric.md`` | YC/Y Combinator |
| [docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md#L894) | 894 | `- `garrytan/repo` → `https://github.com/garrytan/repo.git`` | Garry/Gary, garrytan |
| [docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/SKILLPACK_REGISTRY_V1_SPEC.md#L1334) | 1334 | `9. **Search**: `modusbrain skillpack search yc --json` returns the entry,` | YC/Y Combinator |
| [docs/designs/V038_SCHEMA_PACKS.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/V038_SCHEMA_PACKS.md#L7) | 7 | `Branch: garrytan/houston-v1 \| Mode: EXPANSION` | Garry/Gary, garrytan |
| [docs/designs/V038_SCHEMA_PACKS.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/V038_SCHEMA_PACKS.md#L32) | 32 | `(6) `~/.modusbrain/config.json schema_pack`, (7) default `gbrain-base`.` | GBrain |
| [docs/designs/V038_SCHEMA_PACKS.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/V038_SCHEMA_PACKS.md#L95) | 95 | `- **Engine layer:** gbrain-base universal starter pack; 5 composable` | GBrain |
| [docs/designs/V038_SCHEMA_PACKS.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/V038_SCHEMA_PACKS.md#L122) | 122 | `- **gbrain-base:** byte-for-byte reproduces today's hardcoded` | GBrain |
| [docs/designs/V038_SCHEMA_PACKS.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/V038_SCHEMA_PACKS.md#L135) | 135 | `gbrain-base reproduces today's values for each.` | GBrain |
| [docs/designs/V038_SCHEMA_PACKS.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/V038_SCHEMA_PACKS.md#L162) | 162 | `gbrain-base equivalence regression. **gbrain-base byte-for-byte` | GBrain |
| [docs/designs/V038_SCHEMA_PACKS.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/designs/V038_SCHEMA_PACKS.md#L164) | 164 | ``test/regressions/gbrain-base-equivalence.test.ts` asserting the` | GBrain |
| [docs/guides/queue-operations-runbook.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/guides/queue-operations-runbook.md#L9) | 9 | `"My queue looks wedged — what do I run?" The commands below are in the order` | First Person |
| [docs/how-modusbrain-works.mdx](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/how-modusbrain-works.mdx#L15) | 15 | `## What ModusBrain Adds (vs. Base GBrain Engine)` | GBrain |
| [docs/how-modusbrain-works.mdx](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/how-modusbrain-works.mdx#L32) | 32 | `ModusBrain introduces two structured page types that don't exist in plain GBrain:` | GBrain |
| [docs/INSTALL.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/INSTALL.md#L13) | 13 | `Already running [OpenClaw](https://github.com/garrytan/openclaw) or [Hermes](https://github.com/garrytan/hermes)?` | Garry/Gary, garrytan |
| [docs/MODUSBRAIN_V0.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/MODUSBRAIN_V0.md#L237) | 237 | `│   "Extracting entities... Paul Graham (person), Y Combinator (company)..."` | YC/Y Combinator |
| [docs/MODUSBRAIN_V0.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/MODUSBRAIN_V0.md#L238) | 238 | `│   "Creating links... Paul Graham → Y Combinator (founded)..."` | YC/Y Combinator |
| [docs/schema-author-tutorial.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/schema-author-tutorial.md#L3) | 3 | `You'll fork the bundled `gbrain-base` pack, add a custom `researcher` page type, import a handful of placeholder researcher pages, backfill their `page.type` column with one command, then prove the wiring works by running `modusbrain whoknows` and seeing your new type surface in results. End state: a forked-and-active pack on disk, ~5 pages typed as `researcher`, and a query that proves the pack-aware routing fires end-to-end.` | GBrain |
| [docs/schema-author-tutorial.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/schema-author-tutorial.md#L27) | 27 | `"pack_name": "gbrain-base",` | GBrain |
| [docs/schema-author-tutorial.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/schema-author-tutorial.md#L42) | 42 | `modusbrain schema fork gbrain-base mine` | GBrain |
| [docs/schema-author-tutorial.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/schema-author-tutorial.md#L45) | 45 | `Output: `Forked 'gbrain-base' → 'mine' at ~/.modusbrain/schema-packs/mine/pack.json`.` | GBrain |
| [docs/schema-author-tutorial.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/schema-author-tutorial.md#L47) | 47 | `The fork is a byte-for-byte copy of `gbrain-base` living at `~/.modusbrain/schema-packs/mine/pack.json`. Now you have a writable pack you can mutate.` | GBrain |
| [docs/schema-author-tutorial.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/schema-author-tutorial.md#L180) | 180 | `- A fork of `gbrain-base` named `mine` at `~/.modusbrain/schema-packs/mine/pack.json`, active in your brain via `~/.modusbrain/config.json`.` | GBrain |
| [docs/storage-tiering.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/storage-tiering.md#L21) | 21 | `- yc/` | YC/Y Combinator |
| [docs/tutorials/company-brain.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/tutorials/company-brain.md#L16) | 16 | `I'm Shubham Chavan, founder of Genthropic. I built ModusBrain to run my own AI agents in production. After a couple of months of multi-user features landing (parallel sync across team sources, per-user OAuth scoping, leak-free isolation across every read path), it's finally usable as a company brain too. This is the recipe I'd run if I were standing it up for a 10-50 person company today.` | First Person |
| [docs/tutorials/company-brain.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/tutorials/company-brain.md#L320) | 320 | `Alongside individual skill directories, drop a few flat `_*-rules.md` files at the root of `skills/`. These are conventions that EVERY skill reads. The ones I run in production:` | First Person |
| [docs/tutorials/company-brain.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/tutorials/company-brain.md#L561) | 561 | `If you're building in this space (which YC has flagged as the [company-brain category in its Request for Startups](https://www.ycombinator.com/rfs#company-brain)), you might as well build on this. Everything described above is open source, MIT licensed, and what I run in production behind my own AI agents.` | YC/Y Combinator, First Person |
| [docs/what-schemas-unlock.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/what-schemas-unlock.md#L7) | 7 | `The default schema (`gbrain-base`) ships with 22 page types covering the universal shapes — people, companies, meetings, notes, daily, calendar events. That's enough to start. But your brain is yours, and your brain's shape is not the default shape. A research brain needs `researcher` and `paper` as first-class types. A founder brain needs `lead`, `investor`, `portco`, `deal-stage`. A lawyer brain needs `case`, `motion`, `deposition`, `precedent`. Same engine, totally different shape.` | GBrain |
| [docs/what-schemas-unlock.md](file:///C:/Users/Shubham/Downloads/gbrain-master/gbrain-master/docs/what-schemas-unlock.md#L47) | 47 | `modusbrain schema fork gbrain-base mine` | GBrain |
